# Dolphin Setup Wizard

Welcome! Let's get your project set up.

## Step 1: Start the setup
Click the **Copy to Cloud Shell** button below and run the command. It should take around a minute to complete

```bash
chmod +x setup.sh && ./setup.sh
```

The next step will appear once this process is completed successfully
